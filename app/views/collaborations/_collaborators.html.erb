<div class="panel panel-info">
  <div class="panel-heading">
    <h4>Collaborators</h4>
  </div>
  <div class="panel-body">
    <% wiki.collaborations.each do |collab| %>
      <h4><span class="glyphicon glyphicon-user" aria-hidden="true"> <%= collab.user.username %> <%= link_to "Remove", [wiki, collab], method: :delete, class: 'btn btn-sm btn-danger', data: { confirm: 'Are you sure you want to remove this user?'} if current_user.is_owner_of?(wiki)%></span></h4>
    <% end %>
    <% if current_user.is_owner_of? wiki %>
      <%= render partial: 'collaborations/form', locals: { wiki: wiki, collaboration: collaboration } %>
    <% end %>
  </div>
</div>